% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lccc.checks.R
\name{empirical_lc}
\alias{empirical_lc}
\title{Estimate an empirical lower cutoff for a length-converted catch curve}
\usage{
empirical_lc(
  lfd,
  smooth = TRUE,
  span = 0.35,
  second_if_bimodal = TRUE,
  peak_rel_min = 0.2,
  lc_mult = 0.9
)
}
\arguments{
\item{lfd}{FLQuant length-frequency object.}

\item{smooth}{Logical. Should relative frequencies be smoothed before finding
peaks? Default is \code{TRUE}.}

\item{span}{Numeric smoothing span passed to \code{stats::loess} when
\code{smooth = TRUE}.}

\item{second_if_bimodal}{Logical. If two or more valid local maxima are
detected, should the second peak in length order be used? Default is \code{TRUE}.}

\item{peak_rel_min}{Numeric between 0 and 1. Minimum relative height for a
local maximum to be considered a peak.}

\item{lc_mult}{Numeric multiplier applied to the selected peak to define the
lower cutoff. The default is \code{0.9}, i.e. the catch curve uses length classes
larger than 90 percent of the selected peak length.}
}
\value{
A list of class \code{flicc_empirical_lc} with elements \code{lc}, \code{Lpeak},
\code{peak_index}, \code{len}, \code{observed}, \code{smoothed}, and \code{peaks}.
}
\description{
Estimates the lower length cutoff used to fit the descending limb of a
length-converted catch curve. The cutoff is based on the empirical modal
length class. If the distribution is bimodal, the second local maximum in
length order can be selected, which is often preferable when the first peak
represents partial recruitment, juveniles, or a recruitment pulse.
}
\details{
The function collapses the length composition over all non-length dimensions,
converts the result to relative frequencies, optionally smooths the relative
frequency curve, finds local maxima, and returns \code{lc = lc_mult * Lpeak}.
}
\examples{
\dontrun{
data(alfonsino)
lfd <- lfd_alfonsino$Gillnet
lhpar <- lhpar_alfonsino

elc <- empirical_lc(lfd)
elc$lc
plot_empirical_lc(elc)
}

}

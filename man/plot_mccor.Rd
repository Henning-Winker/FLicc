% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcflicc.R
\name{plot_mccor}
\alias{plot_mccor}
\title{Plot correlations among Monte Carlo life-history parameter draws}
\usage{
plot_mccor(
  mc,
  pairs = list(c("linf", "k"), c("M", "k"), c("linf", "L50"), c("linf", "CVL")),
  dAIC_cut = 20,
  ncol = 2
)
}
\arguments{
\item{mc}{An object returned by \code{mc_flicc()}.}

\item{pairs}{A list of character vectors of length two defining parameter
pairs to plot. Defaults to \code{list(c("linf", "k"), c("M", "k"),
  c("linf", "L50"), c("linf", "CVL"))}.}

\item{dAIC_cut}{Delta AIC threshold used to define the retained plausible
ensemble. Default is \code{20}.}

\item{ncol}{Number of columns in the faceted plot. Default is \code{2}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces faceted scatter plots for selected pairs of life-history parameters
sampled during a FLicc Monte Carlo uncertainty run. Points are classified by
delta AIC support. Fits with \code{dAIC <= dAIC_cut} are shown using a colour
gradient, while less-supported fits are shown as open grey circles. The
original fitted parameter combination stored in \code{mc$fit$lhpar} is shown
as a blue star.
}
\examples{
\dontrun{
mc <- mc_flicc(fit, nsim = 500)
plot_mccor(mc, dAIC_cut = 20)
}
}

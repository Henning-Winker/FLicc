% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flicc_tmb.R
\name{flicc_convergence}
\alias{flicc_convergence}
\title{Summarise convergence diagnostics for a FLicc fit}
\usage{
flicc_convergence(
  fit,
  grad_tol = 0.01,
  eig_tol = 1e-08,
  optimizer.code = FALSE
)
}
\arguments{
\item{fit}{A fitted FLicc model object, typically returned by \code{fiticc()}.
The object is expected to contain \code{fit$opt}, \code{fit$obj}, \code{fit$par}, and
either \code{fit$rep$pdHess} or \code{fit$sdrep$pdHess}.}

\item{grad_tol}{Numeric. Tolerance used to judge whether the maximum absolute
gradient is sufficiently small. Default is \code{1e-3}.}

\item{eig_tol}{Numeric. Tolerance used to judge whether Hessian eigenvalues
are positive. Default is \code{1e-8}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{check}{Name of the convergence diagnostic.}
\item{value}{Observed diagnostic value.}
\item{passed}{Logical flag indicating whether the check passed.}
\item{interpretation}{Plain-language explanation of the check.}
}
}
\description{
Provides a compact, lay-readable convergence summary for a fitted FLicc/TMB
model. The function checks whether the optimiser terminated normally, whether
the maximum absolute gradient is small, whether the Hessian is positive
definite, whether standard errors are available, and whether the numerical
Hessian has positive eigenvalues.
}
\details{
Optimiser convergence alone does not guarantee a reliable fit. A model may
stop with optimiser code \code{0} but still have a large gradient, indicating that
the solution is not close to a stationary point. Conversely, standard errors
and a positive-definite Hessian indicate that local curvature is estimable,
but should still be interpreted together with the gradient check.

The Hessian positive-definiteness check first looks for \code{fit$rep$pdHess},
then for \code{fit$sdrep$pdHess}. The eigenvalue check recomputes a numerical
Hessian using \code{stats::optimHess()}.
}
\examples{
\dontrun{
fit <- fiticc(lfds, stock, lhpar)
flicc_convergence(fit)
}

}

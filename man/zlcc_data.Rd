% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lccc.checks.R
\name{zlcc_data}
\alias{zlcc_data}
\title{Build regression data for a length-converted catch curve}
\usage{
zlcc_data(
  lfd,
  lhpar,
  lc = NULL,
  empirical_lc_args = list(),
  max_rel_linf = 0.999
)
}
\arguments{
\item{lfd}{An FLQuant with length in the first dimension.}

\item{lhpar}{Life-history parameters containing at least \code{linf}
and \code{k}.}

\item{lc}{Optional lower length cutoff. If \code{NULL}, estimated with
\code{empirical_lc()}.}

\item{empirical_lc_args}{Optional list of arguments passed to
\code{empirical_lc()}.}

\item{max_rel_linf}{Maximum allowed length relative to Linf.}
}
\value{
A data.frame with columns \code{len}, \code{year}, \code{unit},
\code{season}, \code{area}, \code{iter}, \code{n}, \code{dt},
\code{y}, and \code{x}.
}
\description{
Constructs the regression data used by a Pauly-style length-converted
catch curve. The response is log(N / Delta t), where N is the observed
length-frequency count and Delta t is the time required to grow through
the length interval. The predictor is relative age from the inverse
von Bertalanffy curve. The intercept is arbitrary; Z is estimated from
the negative slope.
}
\examples{
\dontrun{
data(alfonsino)
lfd <- lfd_alfonsino$Gillnet
lhpar <- lhpar_alfonsino

dat <- zlcc_data(lfd, lhpar)
head(dat)
plot_zlcc(lfd, lhpar)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcflicc.R
\name{plot_mcsprdist}
\alias{plot_mcsprdist}
\title{Plot likelihood-weighted SPR distributions}
\usage{
plot_mcsprdist(
  mc,
  years = NULL,
  dAIC_cut = 20,
  scale = 0.45,
  n = 300,
  bw = 0.04,
  trim = 0.01,
  show_fit = TRUE
)
}
\arguments{
\item{mc}{An object returned by \code{mc_flicc()}.}

\item{years}{Optional vector of years to plot. If \code{NULL}, all years in
\code{mc$spr} are used.}

\item{dAIC_cut}{Optional delta AIC cutoff used before calculating Akaike
weights. If \code{NULL}, all iterations are weighted by their relative
AIC support.}

\item{scale}{Width scaling factor for density polygons. Default is
\code{0.45}.}

\item{n}{Number of points used to evaluate each density. Default is
\code{300}.}

\item{bw}{Bandwidth passed to \code{stats::density()}. Setting this explicitly
avoids warnings because automatic bandwidth selection does not use weights.
Default is \code{0.04}.}

\item{trim}{Relative density cutoff used to remove very small density tails.
For example, \code{trim = 0.02} only draws parts of the density greater
than two percent of the yearly maximum density. Default is \code{0.01}.}

\item{show_fit}{Logical. If \code{TRUE}, show the original fitted SPR values
from \code{mc$fit$spr}.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces right-sided density plots of likelihood-weighted spawning potential
ratio (SPR) distributions across years. The red point and vertical line show
the weighted median and uncertainty interval, while the blue star shows the
original fitted model stored in \code{mc$fit}.
}
